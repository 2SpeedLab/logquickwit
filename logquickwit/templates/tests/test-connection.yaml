apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "logquickwit.fullname" . }}-test-connection"
  labels:
    {{- include "logquickwit.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "logquickwit.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
